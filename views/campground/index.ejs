<% layout('layout/boilerplate') %>
<div class="container">
	<h1>Testing Campgrounds page</h1>
	<ul>
		<% for(let campground of campgrounds){ %>
		<li>
			<a class="link-secondary" href="/campgrounds/<%=campground.id%> ">
				<%=campground.title%></a
			>
		</li>
		<% } %>
	</ul>
</div>
